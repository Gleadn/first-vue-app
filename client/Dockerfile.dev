# Dockerfile de développement pour le client Vue.js
FROM node:20-alpine

WORKDIR /app

# Installer les dépendances
COPY package*.json ./
RUN npm install

# Exposer le port de développement Vite
EXPOSE 5173

# Démarrer en mode développement avec hot reload
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]